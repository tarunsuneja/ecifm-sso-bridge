apiVersion: v1
kind: Service
metadata:
  name: ecifm-saml-bridge
  namespace: tririga
spec:
  selector:
    app: ecifm-saml-bridge
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
